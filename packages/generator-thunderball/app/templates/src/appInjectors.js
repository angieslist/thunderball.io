module.exports = {
  // beforePageMounted: () => {
  //   // Fires before a page starts rendering
  // },
  // afterPageMounted: (store) => {
  //   // Fires before page a page initially rendered
  // },
  // beforeCreateRoutes: () => {
  //   // Fires before routes are created for a page
  // },
  // afterCreateRoutes: () => {
  //   // Fires after routes are created for a page
  // },
  //
  // beforeConfigureStore: (options) => {
  //   // Fires before the redux store is created for a page
  //   return options;
  // },
  // afterConfigureStore: (store) => {
  //   // Fires after the redux store is created for a page
  // }
};
